# Azure Web App workflow disabled for HELPORA.
# HELPORA is a native SwiftUI iOS project; this Node/Azure workflow is not used.

name: Azure Web App (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Azure Web App workflow is disabled for the native iOS HELPORA project."
